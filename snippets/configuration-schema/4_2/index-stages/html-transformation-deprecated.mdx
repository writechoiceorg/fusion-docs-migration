export const schema = {
  "type": "object",
  "title": "HTML Transformation (deprecated)",
  "description": "This stage transforms HTML using JSoup SDK",
  "properties": {
    "skip": {
      "type": "boolean",
      "title": "Skip This Stage",
      "description": "Set to true to skip this stage.",
      "default": false,
      "hints": [
        "advanced"
      ]
    },
    "label": {
      "type": "string",
      "title": "Label",
      "description": "A unique label for this stage.",
      "hints": [
        "advanced"
      ],
      "maxLength": 255
    },
    "condition": {
      "type": "string",
      "title": "Condition",
      "description": "Define a conditional script that must result in true or false. This can be used to determine if the stage should process or not.",
      "hints": [
        "code",
        "code/javascript",
        "advanced"
      ]
    },
    "recordSelector": {
      "type": "string",
      "title": "Record Selector"
    },
    "parentIdField": {
      "type": "string",
      "title": "Parent ID Field Name"
    },
    "bodyField": {
      "type": "string",
      "title": "Body Field Name",
      "default": "body"
    },
    "mappings": {
      "type": "array",
      "title": "HTML Mappings",
      "items": {
        "type": "object",
        "required": [
          "selectRule",
          "field"
        ],
        "properties": {
          "selectRule": {
            "type": "string",
            "title": "Select Rule"
          },
          "attribute": {
            "type": "string",
            "title": "Attribute"
          },
          "field": {
            "type": "string",
            "title": "Field"
          },
          "multivalue": {
            "type": "boolean",
            "title": "Multi Value",
            "default": false
          }
        }
      }
    },
    "metadata": {
      "type": "array",
      "title": "Additional Metadata",
      "items": {
        "type": "object",
        "required": [
          "field",
          "value"
        ],
        "properties": {
          "field": {
            "type": "string",
            "title": "Field"
          },
          "value": {
            "type": "string",
            "title": "Value"
          }
        }
      }
    },
    "keepParent": {
      "type": "boolean",
      "title": "Should Keep Parent Document?",
      "default": false
    }
  },
  "category": "Deprecated",
  "categoryPriority": 1,
  "unsafe": false
}
