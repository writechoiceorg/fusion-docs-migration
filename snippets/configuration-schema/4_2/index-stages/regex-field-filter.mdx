export const schema = {
  "type": "object",
  "title": "Regex Field Filter",
  "description": "This stage allows you to filter fields by matching on field values using regular expressions",
  "properties": {
    "skip": {
      "type": "boolean",
      "title": "Skip This Stage",
      "description": "Set to true to skip this stage.",
      "default": false,
      "hints": [
        "advanced"
      ]
    },
    "label": {
      "type": "string",
      "title": "Label",
      "description": "A unique label for this stage.",
      "hints": [
        "advanced"
      ],
      "maxLength": 255
    },
    "condition": {
      "type": "string",
      "title": "Condition",
      "description": "Define a conditional script that must result in true or false. This can be used to determine if the stage should process or not.",
      "hints": [
        "code",
        "code/javascript",
        "advanced"
      ]
    },
    "reservedFieldsFilteringAllowed": {
      "type": "boolean",
      "title": "Allow System Fields Filtering?",
      "default": false
    },
    "filters": {
      "type": "array",
      "title": "Filters",
      "items": {
        "type": "object",
        "required": [
          "sourceField",
          "pattern"
        ],
        "properties": {
          "sourceField": {
            "type": "string",
            "title": "Source Field"
          },
          "pattern": {
            "type": "string",
            "title": "Filter Pattern",
            "description": "A regular expression to test the field against.  The pattern must match the field in its entirety.",
            "format": "regex"
          },
          "exclusive": {
            "type": "boolean",
            "title": "Filter is Exclusive?",
            "description": "Exclude only matching values when true, otherwise include only matching values.",
            "default": true
          }
        }
      }
    }
  },
  "category": "Field Transformation",
  "categoryPriority": 6,
  "unsafe": false
}
