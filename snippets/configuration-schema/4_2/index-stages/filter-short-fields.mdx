export const schema = {
  "type": "object",
  "title": "Filter Short Fields",
  "description": "Filter for removing short fields based on the length of the field values",
  "properties": {
    "skip": {
      "type": "boolean",
      "title": "Skip This Stage",
      "description": "Set to true to skip this stage.",
      "default": false,
      "hints": [
        "advanced"
      ]
    },
    "label": {
      "type": "string",
      "title": "Label",
      "description": "A unique label for this stage.",
      "hints": [
        "advanced"
      ],
      "maxLength": 255
    },
    "condition": {
      "type": "string",
      "title": "Condition",
      "description": "Define a conditional script that must result in true or false. This can be used to determine if the stage should process or not.",
      "hints": [
        "code",
        "code/javascript",
        "advanced"
      ]
    },
    "filters": {
      "type": "array",
      "title": "Filters",
      "items": {
        "type": "object",
        "required": [
          "sourceField",
          "length"
        ],
        "properties": {
          "sourceField": {
            "type": "string",
            "title": "Source Field"
          },
          "length": {
            "type": "integer",
            "title": "Minimum Length",
            "description": "Minimum length of permissible entities. Fields with lesser values will be removed.",
            "default": 3,
            "minimum": 1,
            "exclusiveMinimum": false
          }
        }
      }
    }
  },
  "category": "Field Transformation",
  "categoryPriority": 6,
  "unsafe": false
}
