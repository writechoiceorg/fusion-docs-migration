export const schema = {
  "type": "object",
  "title": "Retrieve Stored Parameters",
  "description": "Make use of parameters stored in a \<collection>_stored_parameters system collection.",
  "properties": {
    "skip": {
      "type": "boolean",
      "title": "Skip This Stage",
      "description": "Set to true to skip this stage.",
      "default": false,
      "hints": [
        "advanced"
      ]
    },
    "label": {
      "type": "string",
      "title": "Label",
      "description": "A unique label for this stage.",
      "hints": [
        "advanced"
      ],
      "maxLength": 255
    },
    "condition": {
      "type": "string",
      "title": "Condition",
      "description": "Define a conditional script that must result in true or false. This can be used to determine if the stage should process or not.",
      "hints": [
        "code",
        "code/javascript",
        "advanced"
      ]
    },
    "key": {
      "type": "string",
      "title": "Key",
      "description": "The parameter name from the context/query whose value will be used to query the stored parameters collection."
    },
    "inherit": {
      "type": "boolean",
      "title": "Inherit Parameters From Parent?",
      "description": "Select box if a child category should inherit its parents' stored parameters.",
      "default": false
    }
  },
  "category": "Advanced",
  "categoryPriority": 2,
  "unsafe": false
}
