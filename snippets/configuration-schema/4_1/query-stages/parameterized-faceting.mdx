export const schema = {
  "type": "object",
  "title": "Parameterized Faceting",
  "description": "Query Solr to determine the most frequent value for a given field (e.g. category) and then use that value to lookup stored parameters to auto-select the most appropriate facet for a query.",
  "required": [
    "facetField",
    "handler",
    "method"
  ],
  "properties": {
    "skip": {
      "type": "boolean",
      "title": "Skip This Stage",
      "description": "Set to true to skip this stage.",
      "default": false,
      "hints": [
        "advanced"
      ]
    },
    "label": {
      "type": "string",
      "title": "Label",
      "description": "A unique label for this stage.",
      "hints": [
        "advanced"
      ],
      "maxLength": 255
    },
    "condition": {
      "type": "string",
      "title": "Condition",
      "description": "Define a conditional script that must result in true or false. This can be used to determine if the stage should process or not.",
      "hints": [
        "code",
        "javascript",
        "advanced"
      ]
    },
    "facetField": {
      "type": "object",
      "title": "Facet Field",
      "required": [
        "field"
      ],
      "properties": {
        "field": {
          "type": "string",
          "title": "Field",
          "description": "The field whose values you want to facet on."
        },
        "prefix": {
          "type": "string",
          "title": "Prefix",
          "description": "Prefix of terms to facet on."
        },
        "sort": {
          "type": "string",
          "title": "Sort",
          "description": "Ordering of the faceting results.",
          "enum": [
            "count",
            "index"
          ]
        },
        "limit": {
          "type": "integer",
          "title": "Limit",
          "description": "Maximum number of facets to return."
        },
        "offset": {
          "type": "integer",
          "title": "Offset",
          "description": "Offset into list of resulting facets."
        },
        "minCount": {
          "type": "integer",
          "title": "Minimum Count",
          "description": "Lower threshold of term counts to be included.",
          "default": 1
        },
        "missing": {
          "type": "boolean",
          "title": "Count Missing",
          "description": "Optionally include a 'missing' facet bucket for documents without the selected field.",
          "default": false
        },
        "method": {
          "type": "string",
          "title": "Method",
          "enum": [
            "enum",
            "fc",
            "fcs"
          ]
        },
        "enumCacheMinDf": {
          "type": "integer",
          "title": "Enum Cache Minimum DF",
          "minimum": 0,
          "exclusiveMinimum": false
        },
        "threads": {
          "type": "integer",
          "title": "Number of Threads",
          "minimum": 0,
          "exclusiveMinimum": false
        }
      }
    },
    "fallbackValue": {
      "type": "string",
      "title": "Fallback Value",
      "description": "Optional fallback value to use for the facet field if one cannot be determined by querying Solr."
    },
    "failQueryOnError": {
      "type": "boolean",
      "title": "Fail Query on Error",
      "description": "Fail the query request if an error occurs when processing this stage.",
      "default": false
    },
    "handler": {
      "type": "string",
      "title": "Request Handler",
      "description": "Solr query request handler to send the auto-facet query request to.",
      "default": "select"
    },
    "method": {
      "type": "string",
      "title": "HTTP Method",
      "description": "HTTP method used to send the auto-facet query request.",
      "enum": [
        "GET",
        "POST"
      ],
      "default": "GET"
    },
    "params": {
      "type": "array",
      "title": "Additional Query Parameters",
      "items": {
        "type": "object",
        "required": [
          "key"
        ],
        "properties": {
          "key": {
            "type": "string",
            "title": "Parameter Name"
          },
          "value": {
            "type": "string",
            "title": "Parameter Value"
          }
        }
      }
    },
    "headers": {
      "type": "array",
      "title": "Headers",
      "items": {
        "type": "object",
        "required": [
          "key"
        ],
        "properties": {
          "key": {
            "type": "string",
            "title": "Parameter Name"
          },
          "value": {
            "type": "string",
            "title": "Parameter Value"
          }
        }
      }
    },
    "inherit": {
      "type": "boolean",
      "title": "Inherit Parameters From Parent?",
      "description": "Select box if a child category should inherit its parents' stored parameters.",
      "default": false
    }
  },
  "category": "Other",
  "categoryPriority": 1
}
