{
  "type": "object",
  "title": "JSON Parsing (deprecated)",
  "description": "This stage parses JSON content from a document field into new documents",
  "required": [
    "splitPath"
  ],
  "properties": {
    "skip": {
      "type": "boolean",
      "title": "Skip This Stage",
      "description": "Set to true to skip this stage.",
      "default": false,
      "hints": [
        "advanced"
      ]
    },
    "label": {
      "type": "string",
      "title": "Label",
      "description": "A unique label for this stage.",
      "hints": [
        "advanced"
      ],
      "maxLength": 255
    },
    "condition": {
      "type": "string",
      "title": "Condition",
      "description": "Define a conditional script that must result in true or false. This can be used to determine if the stage should process or not.",
      "hints": [
        "code",
        "code/javascript",
        "advanced"
      ]
    },
    "sourceField": {
      "type": "string",
      "title": "Source Field",
      "description": "The document field where the JSON content should be read from.",
      "default": "_raw_content_"
    },
    "sourceEncoding": {
      "type": "string",
      "title": "Source Field Encoding",
      "description": "The content transport encoding of the data stored in the source field (per RFC1341).",
      "enum": [
        "binary",
        "base64"
      ],
      "default": "binary"
    },
    "inheritFields": {
      "type": "boolean",
      "title": "Inherit Fields",
      "description": "Should generated documents include the original document's fields.",
      "default": false
    },
    "splitPath": {
      "type": "string",
      "title": "JSON Split Point",
      "default": "/"
    },
    "mappingRules": {
      "type": "array",
      "title": "JSON Mapping Rules",
      "items": {
        "type": "object",
        "required": [
          "path"
        ],
        "properties": {
          "path": {
            "type": "string",
            "title": "JSON Path to Extract"
          },
          "field": {
            "type": "string",
            "title": "Destination Field Name",
            "description": "If omitted, will default to the extracted JSON property."
          }
        }
      }
    }
  },
  "category": "Deprecated",
  "categoryPriority": 1,
  "unsafe": false
}
